/**
 * 数组中的第 K 个最大元素
 *
 * 给定整数数组 nums 和整数 k，返回数组中第 k 大的元素。
 *
 * 注意：是排序后的第 k 个元素，不是第 k 个不同元素。
 *
 * 例如：
 *
 * nums = [3, 2, 1, 5, 6, 4];
 * k = 2;
 *
 * 降序排列：
 *
 * [6, 5, 4, 3, 2, 1]
 *
 * 结果：
 *
 * 5
 *
 * 再例如：
 *
 * nums = [3, 2, 3, 1, 2, 4, 5, 5, 6];
 * k = 4;
 *
 * 结果是：
 *
 * 4
 */

/**
 *
 * @param nums
 * @param k
 */
function findKthLargestMinHeap(nums, k) {

}